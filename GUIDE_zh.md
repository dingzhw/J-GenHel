# J-GenHel 新手快速上手指南

欢迎来到 J-GenHel 项目！本指南旨在帮助初学者，特别是那些刚接触 Julia 语言或计算流体动力学（CFD）的用户，快速理解并运行这个项目。

## 目录
1.  [引言：J-GenHel 是什么？](#1-引言j-genhel-是什么)
2.  [第一步：环境搭建](#2-第一步环境搭建)
    *   [2.1 安装 Julia](#21-安装-julia)
    *   [2.2 获取项目代码](#22-获取项目代码)
    *   [2.3 安装项目依赖](#23-安装项目依赖)
3.  [项目结构详解](#3-项目结构详解)
4.  [核心流程：运行仿真](#4-核心流程运行仿真)
    *   [4.1 脚本概览](#41-脚本概览)
    *   [4.2 配平与线性化](#42-配平与线性化)
    *   [4.3 特征值分析](#43-特征值分析)
    *   [4.4 频率响应分析](#44-频率响应分析)
    *   [4.5 时域仿真](#45-时域仿真)
5.  [如何运行测试](#5-如何运行测试)
6.  [理解仿真结果](#6-理解仿真结果)
7.  [结语](#7-结语)

---

## 1. 引言：J-GenHel 是什么？

J-GenHel 是一个用 Julia 语言编写的、开源的直升机飞行动力学仿真模型。它模拟了一架类似于 UH-60“黑鹰”直升机的复杂行为。这个项目不仅仅是一个仿真器，它还是一个强大的分析工具，可以用于：

*   计算直升机在特定飞行状态下的**配平**（即稳定飞行的控制量）。
*   将复杂的非线性模型**线性化**，以便进行控制系统设计。
*   分析模型的**稳定性和动态特性**（通过特征值和频率响应）。
*   在**时域**内模拟直升机对控制输入的响应。

对于航空航天专业的学生、研究人员或爱好者来说，这是一个极佳的学习和研究平台。

## 2. 第一步：环境搭建

要运行此项目，您需要先在您的计算机上设置好 Julia 开发环境。

### 2.1 安装 Julia

Julia 是一种现代化、高性能的科学计算语言。
1.  **下载**: 访问 [Julia 官方网站](https://julialang.org/downloads/)，下载适合您操作系统（Windows, macOS, Linux）的最新稳定版。
2.  **安装**: 按照官方指南进行安装。通常情况下，只需运行安装程序并遵循提示即可。
3.  **验证**: 安装完成后，打开您的终端（或 Windows 上的 PowerShell/CMD），输入 `julia` 并按回车。如果您看到了 Julia 的欢迎横幅和 `julia>` 提示符，说明安装成功。

### 2.2 获取项目代码

您可以使用 Git 从代码仓库克隆本项目。
```bash
git clone <repository_url>  # 请将 <repository_url> 替换为实际的项目地址
cd J-GenHel                 # 进入项目目录
```

### 2.3 安装项目依赖

本项目依赖多个 Julia 包来进行数学计算、文件读写和绘图。
1.  **启动 Julia REPL**: 在项目根目录（`J-GenHel/`）下，打开终端并运行 `julia`。
2.  **进入包管理器**: 在 `julia>` 提示符后，按 `]` 键，提示符会变为 `(@v1.x) pkg>`。
3.  **激活项目环境**: 运行以下命令，这将确保依赖项安装在项目本地，而不是全局环境中。
    ```julia
    activate .
    ```
4.  **安装依赖**: 运行以下命令来安装所有必需的包。
    ```julia
    add LinearAlgebra SparseArrays ControlSystems MAT Interpolations ApproxFun Plots JLD
    ```
    包管理器会自动下载并安装所有列出的包及其依赖项。完成后，按 `Backspace` 或 `Ctrl+C` 退出包管理器，返回到 `julia>` 提示符。

至此，您的环境已准备就绪！

## 3. 项目结构详解

了解项目的文件组织方式有助于您更好地理解代码。
```
J-GenHel/
│
├── data/              # 存放模型所需的输入数据
│   ├── AeroInteraction.mat
│   └── ... (其他 .mat 和 .jld 文件)
│
├── output/            # 存放仿真输出结果 (默认)
│   └── ...
│
├── src/               # 核心源代码
│   ├── main.jl        # 主运行脚本
│   ├── GenHel.jl      # 核心直升机动力学模型
│   ├── H60_constants.jl # 模型物理常数
│   ├── trimmer.jl     # 配平计算器
│   ├── linearize.jl   # 线性化工具
│   └── ... (其他辅助函数模块)
│
├── test/              # 测试文件
│   ├── runtests.jl    # 测试主入口
│   ├── test_utils.jl  # 单元测试
│   └── test_integration.jl # 集成测试
│
├── README.md          # 项目简介 (英文)
└── GUIDE_zh.md        # 本指南
```
*   `data/`: 存放着定义直升机气动特性的数据表（`.mat` 文件）。这些数据是仿真的基础。
*   `output/`: 当您运行仿真时，生成的 `.jld` 数据文件会默认保存在这里。
*   `src/`: 这是最重要的目录，包含了所有的功能代码。`main.jl` 是您运行分析和仿真的入口。
*   `test/`: 存放自动化测试脚本，用于验证代码的正确性。

## 4. 核心流程：运行仿真

项目的主入口是 `src/main.jl`。这个脚本串联了所有的核心分析步骤。

### 4.1 脚本概览

要运行整个流程，只需在 Julia REPL 中执行：
```julia
# 确保您在 src 目录下，或者调整 include 的路径
include("src/main.jl")
```
这个命令会依次执行以下步骤：

### 4.2 配平与线性化

*   **配平 (Trimming)**: 脚本首先会为一个设定的飞行状态（例如，80节的前飞速度）计算直升机的配平条件。这意味着它会找出需要施加多大的控制输入（周期变距、总距、尾桨距）才能让直升机稳定在该状态下飞行，即所有的力和力矩都达到平衡。
*   **线性化 (Linearization)**: 在找到配平点后，脚本会将复杂的非线性模型在该点附近简化为一个线性的状态空间模型 (`A`, `B` 矩阵)。线性模型对于飞行控制系统设计至关重要。

### 4.3 特征值分析

脚本会计算全阶模型和降阶后模型的特征值，并绘制在复平面上。
*   **特征值 (Eigenvalues)**: 它们代表了系统的“模态”。通过观察特征值的位置，可以判断系统的稳定性。如果所有特征值的实部都为负，则系统是稳定的。

### 4.4 频率响应分析

接下来，脚本会生成伯德图 (Bode Plot)。
*   **伯德图**: 它显示了系统对不同频率正弦输入信号的响应。这有助于分析直升机在不同频率下的动态响应特性，例如，飞行员的操纵响应或对阵风的响应。

### 4.5 时域仿真

最后，脚本会进行两种时域仿真：
*   **开环仿真 (Open-Loop)**: 模拟直升机在没有飞控系统辅助下的响应。通常会表现出不稳定或难以控制的特性。
*   **闭环仿真 (Closed-Loop)**: 模拟带有动态逆控制器（一种高级控制策略）的直升机响应。您会看到闭环系统的性能远优于开环系统。

## 5. 如何运行测试

我们为项目编写了一套测试，以确保代码的正确性和稳定性。
1.  **启动 Julia REPL**: 在项目根目录 (`J-GenHel/`) 下，打开终端并运行 `julia`。
2.  **进入包管理器**: 按 `]` 进入包管理器模式。
3.  **激活环境并测试**:
    ```julia
    activate .
    test
    ```
    `test` 命令会自动查找 `test/runtests.jl` 文件并执行它。您会看到一系列测试的输出结果。如果所有测试都通过，说明您的代码库是健康的。

## 6. 理解仿真结果

运行 `src/main.jl` 后，您会看到几个弹出的绘图窗口：
1.  **特征值图**: 显示了三种模型（全阶、10状态、8状态）的特征值分布。您可以比较不同模型的稳定性。
2.  **频率响应图 (伯德图)**: 比较了三种模型从横向杆输入到滚转角速度的频率响应。您可以看到降阶模型在多大程度上能逼近全阶模型的特性。
3.  **姿态角响应图**: 对比了开环和闭环仿真中，直升机的滚转角、俯仰角和偏航角随时间的变化。您可以直观地看到控制器如何改善了飞机的稳定性。
4.  **角速率响应图**: 同上，但显示的是角速率的变化。

## 7. 结语

恭喜您！您现在已经了解了如何搭建 J-GenHel 的环境、它的代码结构、如何运行仿真和测试，以及如何解读结果。

这只是一个开始。您可以尝试：
*   修改 `src/main.jl` 中的配平条件（例如，改变飞行速度 `VXTRIM`）。
*   深入研究 `src/GenHel.jl` 中的物理模型。
*   在 `src/DI_design.jl` 中调整控制器参数，看看它如何影响闭环响应。

祝您探索愉快！
